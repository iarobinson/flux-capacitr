<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <h1 style="text-align: center">
      <%= link_to @post.blog.title, blog_url(@post.blog) %>
    </h1>
    <h5 style="text-align: center">
      (<%= "#{request.domain}/#{@post.blog.url}" %>)
    </h5>
    <hr>
    <%= render 'post', post: @post %>
  </div>
</div>