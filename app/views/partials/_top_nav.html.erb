<nav class="navbar custom-nav">
  <div class="container-fluid">
    <div class="navbar-header">
      <!-- <h1>Allons-y!</h1> -->
      <%= link_to root_url do %>
        <%= image_tag("logo.png") %>
      <% end %>
    </div>
    
    <ul class="nav navbar-nav navbar-right">
      <% if logged_in? %>
        <li>
          <%= link_to session_url,
                      method: :delete,
                      class: 'btn btn-danger' do %>
          <span class="glyphicon glyphicon-off"></span>
          <% end %>
        </li>
      <% else %>
        <li>
          <%= link_to 'Sign In',
                        new_session_url,
                        class: 'btn btn-success',
                        method: :get %>
        </li>
        <li>
          <%= link_to 'Register',
                        new_user_url,
                        class: 'btn btn-info',
                        method: :get %>
        </li>
        <li>
          <%= link_to 'Sign In As Guest',
                      session_guest_url,
                      class: 'btn btn-info',
                      method: :get %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>