<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="javascript:void(0)">Allons-y</a>
    </div>
    <div class="pull-right">
      <% if logged_in? %>
        <%= link_to 'Sign Out', session_url, method: :delete, class: 'btn btn-danger' %>
      <% else %>
        <%= link_to 'Sign In', new_session_url, class: 'btn btn-success' %>
        <%= link_to 'Register', new_user_url, class: 'btn btn-info' %>
      <% end %>
    </div>
  </div>
</nav>