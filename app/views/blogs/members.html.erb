<div class="centered-content">
  <%= render 'partials/top_nav' %>
  <%= render 'partials/errors' %>
</div>

<!-- Button trigger modal -->
<button class="btn btn-primary btn-lg" data-toggle="modal" data-target="#addMemberModal">
  Launch demo modal
</button>

<!-- Modal -->
<%= render 'add_member_modal' %>